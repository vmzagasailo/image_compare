/// Model for resolution of image
class ImageResolution {
  /// width of image
  final int? width;
  /// height of image
  final int? height;

  /// Pass width and height to constructor
  const ImageResolution(this.width, this.height);
}
